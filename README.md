
  # Color Change on Click

  This is a code bundle for Color Change on Click. The original project is available at https://www.figma.com/design/30l5OEj2cyYJqPC57MlUR9/Color-Change-on-Click.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  